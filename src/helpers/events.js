export const UPDATE = {
    DATA: 'update:data',
    COLOR: 'update:color',
    KEY: 'update:key'
};

export const ERROR = {
    EMPTY: 'error:empty',
    NUMBER: 'error:number',
    UNIQ: 'error:uniq'
};

Object.freeze(UPDATE);
Object.freeze(ERROR);
